<% provide(:title, 'Enregistrement') %>
<h1>Enregistrement</h1>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(@user) do |i| %>
			<%= render 'shared/error_messages' %>

			<%= i.label :name, "Pseudo" %>
			<%= i.text_field :name %>

			<%= i.label :email, "Adresse e-mail" %>
			<%= i.email_field :email %>

			<%= i.label :email, "Mot de passe" %>
			<%= i.password_field :password %>

			<%= i.label :password_confirmation, "Confirmation" %>
			<%= i.password_field :password_confirmation %>
			
			<%= i.submit "Créer mon compte"%>
		<% end %>
	</div>
</div>